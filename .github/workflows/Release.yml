name: Windows

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
    - uses: ilammy/msvc-dev-cmd@v1
    - name: WhereAmI
      run: Get-Location
    - name: Create release
      run: |
        cmd /c "md rvlq-windows_$tag"
        cmd /c "copy src/* rvlq-windows_$tag/"
        cmd /c "7z a rvlq-windows_$tag.zip rvlq-windows_$tag"
    - name: Upload binaries to release
      uses: svenstaro/upload-release-action@v2
      with:
        repo_token: ${{ secrets.GITHUB_TOKEN }}
        file: rvlq-windows_$tag.zip
#        asset_name: rvlq_windows$tag.xcfg
        tag: 20010101
        overwrite: true
        body: "This is my release text"