name: Windows

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
    - uses: ilammy/msvc-dev-cmd@v1
    - name: WherAmI
      run: echo The current directory is %CD%
    - name: build
      run: MSBuild rvlq.vcxproj
    - name: version
      run: D:\a\rvlq-cli\rvlq-cli\rvlq.exe --version
